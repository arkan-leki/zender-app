import { useContext } from 'react'
import { APIContext } from '../../helper/APIContext'

const Manager = () => {
    const { sell, addSell } = useContext(APIContext)

    return (
        <>
        </>
    )
}

export default Manager
